# a = list(input())
# b = list(input())

# a_len = len(a)
# b_len = len(b)

# dp = [0] * max(a_len, b_len)

# for i in range(a_len):
#     for j in range(b_len):
#         if a[i] == b[j]:
#             dp[j] = max(dp[j] + 1, dp[i])
# print(dp)